# 终末地基质全收集管理器

> 《明日方舟：终末地》基质全收集追踪与刷取推荐工具

网页版工具，帮助玩家追踪全部 840 种基质组合（5 基础 × 12 附加 × 14 技能）的收集进度，动态推荐最优刷取策略，并提供武器图鉴追踪与定向刷取。

**在线使用**：https://lobo918.github.io/zmd-matrix-manager/

## 功能

- **热力图**：12 附加 × 14 技能 = 168 格，每格 5 个小方块对应 5 个基础属性
- **推荐算法**：枚举所有启用刷取点的锁附加/锁技能策略，按加权得分排序
- **多副本管理**：支持同一基质保留多份（多把武器共用一个基质）
- **武器图鉴**：62 把武器毕业进度追踪，三色状态
- **详细清单**：按技能属性分块的矩阵视图，点击格子可修改等级
- **缺口清单**：按来源数升序排列未收集组合，标出每个缺口的具体来源
- **刷取历史**：最近 7 天柱状图 + 分页列表 + 撤销 + 导出
- **ETA 预估**：分段模拟估算还需多少次提交才能全收集
- **数据管理**：导出/导入 JSON、快照、校验修复
- **PWA**：可安装为桌面/主屏应用，支持离线使用

## 快速开始

### 在线使用

直接访问：https://lobo918.github.io/zmd-matrix-manager/

## 项目结构

```
仓库根/
├── index.html              主文件（HTML + CSS + JS 全内联）
├── manifest.json           PWA 清单
├── sw.js                   Service Worker
├── icon-192.png            图标
├── icon-512.png            图标
├── icon-maskable.png       Android 自适应图标
├── data/
│   ├── weapons.json        武器库（数组）
│   └── sites.json          刷取点（{regions: [...]}）
├── README.md               本文件
├── JSON-FORMAT.md          导入导出 JSON 格式规范
└── .gitignore
```

## 数据存储

- 所有数据存在浏览器 **localStorage**，不上传服务器
- 主数据 key：`zmd-optimizer-v10`（历史遗留命名，勿改）
- 主题偏好 key：`zmd-theme`
- 快照 key：`zmd-optimizer-v10_snap_<时间戳>`

**重要**：数据不会跨设备/浏览器同步。换设备需要用「导出 → 导入」。

## 数据备份

设置页「数据管理」提供：

| 操作 | 说明 |
|---|---|
| 一键复制 JSON | 完整备份（含历史）复制到剪贴板 |
| 导出完整备份 | 含历史，用于换设备完整迁移 |
| 导出精简版 | 不含历史，文件更小，导入时保留当前历史 |
| 导入 JSON | 覆盖当前数据（导入前自动打快照） |
| 创建快照 | 手动打快照，保留最近 3 份 |
| 恢复默认刷取点 | 覆盖为仓库里 `data/sites.json` 内容 |
| 重置全部 | 清空所有数据 |

导入文件格式见 [JSON-FORMAT.md](./JSON-FORMAT.md)。

**破坏性操作**（重置全部 / 恢复默认刷取点 / 清空待删除 / 清空历史 / 导入 / 校验修复）前会自动打快照。

## 已知限制

- **手机端未适配**：布局针对桌面/平板优化。手机竖屏下可能出现热力图横向滚动、录入区元素拥挤、设置页换行混乱等问题。**建议在桌面浏览器或平板横屏使用**。
- **跨设备数据不共享**：localStorage 只在单台设备/浏览器内。换设备需要手动导出/导入 JSON。
- **`file://` 协议下不可用**：必须通过 HTTP(S) 访问。
- **主题不随导入导出迁移**：主题是浏览器本地偏好。
- **iPad PWA 限制**：iPadOS 对 PWA 的支持不如 Android 完整，离线缓存偶尔不稳定。

## 技术栈

- 单 HTML 文件 + 外置 JSON
- 无框架、无构建步骤、无外部依赖
- 原生 ES6+
- PWA（Service Worker + Manifest）
- 响应式布局，桌面/平板优先

## 许可

个人工具，仅供学习交流。游戏素材版权归鹰角网络所有。